<header>

  <% if !@principal.blank? && !@principal.nil? %>
      <a href="<%= principal_path %>">
        <%= image_tag @principal.logo_image.url(:medium), :alt => @principal.title %>
      </a>
  <% else %>
      <% ContactMailer.alert_when_error_should_never_happen("Não tem nenhuma página principal ativa cadastrada - HEADER!!!").deliver %>
  <% end %>

      <hr/>

      <!--<div>-->
      <!-- todo futuro - buscar à medida q a pessoa vai digitando - oninput? -->
      <!-- <input type="search" placeholder="Nome do Estabelecimento"/> -->
      <!--</div>-->

      <!--<hr/>-->

</header>